;;; package --- Summary
;;; Commentary:
;;; Setup projectile
;;; Code:
(require 'ido)

(setq ido-enable-flex-matching t)
(setq ido-everywhere t)
(setq ido-create-new-buffer 'always)
(ido-mode t)
;;; ido.el ends here